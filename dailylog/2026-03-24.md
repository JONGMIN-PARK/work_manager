# 데일리 로그 — 2026-03-24

## 작업 요약
서버 배포 환경(Render.com + Supabase) 안정화 및 속도 최적화 대규모 작업

## 완료 작업

### 1. GitHub Pages 배포 설정
- `gh-pages` 활성화 (main 브랜치, / 경로)

### 2. 주차 선택 UI 개선
- 연도/분기(1Q~4Q)/월 계층 필터 드롭다운 추가
- "✔ 기간 선택" 버튼 → 기간 내 주차 일괄 선택 + accent 테두리 강조
- `pickPeriod()` 함수로 selWeek에 날짜 범위 반영

### 3. 서버 모드 CRUD 버그 수정 (핵심)
- **근본 원인**: 클라이언트에서 id를 미리 생성 → `*Put`이 PUT(수정) 시도 → 서버에 없어 404
- **해결**: 모든 `*Put` 함수에 `_isNew` 플래그 + PUT 404→POST 폴백
- 영향 범위: projPut, msPut, evtPut, issuePut, chkPut, folderPut, filePut
- `createProjectFromOrder` 마일스톤 생성 순서 수정 (FK 제약 위반)

### 4. 체크리스트 스키마 불일치 해결
- 서버: phase별 1 row + items JSONB 배열
- 클라이언트: 개별 항목별 row 기대
- `chkGetByProject`에서 items→flat 변환 레이어 구현
- `toggleCheckItem`, `chkDel`, `createCheckItem` 서버 모드 오버라이드

### 5. 속도 최적화
- dashboard.js: 4곳 순차 API → Promise.all 병렬
- timeline.js: showProjectDetail 3개 순차 → 병렬
- calendar.js: issueGetAll을 Promise.all에 통합
- server/app.js: HTML 메모리 캐시, 정적 파일 maxAge 30d
- CDN 라이브러리(Chart.js/XLSX/PDF.js/JSZip) defer 추가
- Dockerfile: Chromium 조건부 설치 (이미지 800MB 축소)
- POST /api/projects/full 일괄 생성 API (13→1 요청)

### 6. 에러 핸들링
- 6개 파일, 총 50+ 곳에 .catch() / try-catch 일괄 추가

### 7. 캐시 버스팅 자동화
- 수동 ?v=N → git commit hash 기반 BUILD_VERSION 자동 치환

### 8. RBAC 완화
- order.edit: manager/PL → 모든 인증 사용자

## 배포
- Render.com (Docker, free plan) — 서버 + 정적 파일
- GitHub Pages — 정적 프론트엔드 (보조)

## 이슈/참고
- Render 무료 플랜: 15분 비활성 시 슬립, 콜드 스타트 30초~2분
- Git Credential Manager(GCM) 손상 → 토큰 기반 push 사용
- 노출된 GitHub 토큰 revoke 필요
